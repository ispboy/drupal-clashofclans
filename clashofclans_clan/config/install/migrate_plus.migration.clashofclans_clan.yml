# Migration configuration for clashofclans_location content.
id: clashofclans_clan
label: 'Clan'
migration_group: 'Clash of Clans'

source:
  plugin: clashofclans_clan
  track_changes: true

destination:
  # Specify the destination plugin (usually entity:entity_type).
  plugin: 'entity:clashofclans_clan'
  default_bundle: 'clashofclans_clan'

process:
  title: name
  field_clan_tag: tag

dependencies:
  module:
    - clashofclans_clan
  enforced:
    module:
      - clashofclans_clan
