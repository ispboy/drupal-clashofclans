# Migration configuration for clashofclans_location content.
id: clashofclans_war
label: 'War'
migration_group: 'Clash of Clans'

source:
  plugin: clashofclans_war
  track_changes: true

destination:
  # Specify the destination plugin (usually entity:entity_type).
  plugin: 'entity:clashofclans_war'
  default_bundle: 'clashofclans_war'

process:
  title: tag
  field_war_tag: tag
  field_data: data

dependencies:
  module:
    - clashofclans_war
  enforced:
    module:
      - clashofclans_war
